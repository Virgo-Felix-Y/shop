<!--index.wxml-->
<!-- <view class="container">
<view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view> 
  </view>
  -->
<view class="section">
  <scroll-view scroll-y class="class_list">
    <view wx:for="{{dishesClassList}}" wx:key="id" wx:for-item="classTitle" id="class_{{classTitle.id}}" class="scroll-view-item" bindtap="getDishesByClassId">
      {{classTitle.name}}
    </view>
  </scroll-view>

  <!-- <view class="btn-area">
    <button size="mini" bindtap="tap">click me to scroll into view</button>
    <button size="mini" bindtap="tapMove">click me to scroll</button>
  </view> -->
  <scroll-view scroll-y class="dishes_list">
    <view wx:for="{{dishesInfoList}}" wx:key="id" wx:for-item="classTitle" id="class_{{classTitle.id}}" class="scroll-view-item">
      {{classTitle.name}}
    </view>
  </scroll-view>
</view>

<!-- <view class="section section_gap">
  <view class="dishes_class_title">欢迎光临</view>
  <scroll-view class="scroll-view_H" scroll-x>
    <view wx:for="{{dishesClassInfo}}" wx:for-item="classTitle" id="class_{{classTitle.id}}" class="scroll-view-item_H">
      {{classTitle.name}}
    </view>
  </scroll-view> 
</view>
-->